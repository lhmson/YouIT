import React from "react";
import { Card, Button, Typography, Row, Col, Space, Input } from "antd";

const { Title, Text, Paragraph } = Typography;

function LoginPage() {
  return (
    <div className="full">
      <Card>
        <Text>Good</Text>
      </Card>
    </div>
  );
}

export default LoginPage;
